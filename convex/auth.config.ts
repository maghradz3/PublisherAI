//https://funky-mastodon-8.clerk.accounts.dev

const authConfig = {
  providers: [
    {
      domain: "https://funky-mastodon-8.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
};

export default authConfig;
